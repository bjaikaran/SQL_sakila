#### IMPORTANT NOTE ON THIS SUBMISSION:
***
*I've opted to join via the **WHERE** clause vince an implicit **INNER JOIN** on any fields as the two had a direct relationship and the were instances where joining the data would result in instances of non-data in either of the two joined tables.*
